let doubleSpeed = function(velocity){
    return velocity * 2
}

let newVelocity = doubleSpeed(40)

console.log(newVelocity)

//funções anonimas por serem atribuidas em variaveis seguem a regra das chamadas de variaveis, por isso ela não pode ser acessada antes da inicialização